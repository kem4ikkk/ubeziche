# ✅ ТРЕКЕР ПРОГРЕССА — «Убежище»

> Живой чек-лист разработки. Обновляется по ходу проекта.
> Концепция и обоснование этапов — в [ROADMAP.md](ROADMAP.md).

## Легенда
- `[ ]` — не начато
- `[~]` — в работе / частично
- `[x]` — готово

## 🔭 Текущий фокус
Ядро ЭТАПА 4 (4.1–4.11) завершено. 4.8.3 (мортира), 4.8.4 (гатлинг), 4.14
(система питания) и 4.15 (тиры убежища) сделаны. Этап 3.8 (плейтест) пропущен
по решению автора — продолжаем углубление обороны/базы из разд. 17.

✅ 2026-06-15: **4.16 Переработка ресурсов** сделана:
- `stone` → `steel` (переименование, "Сталь" в HUD) — второй стройматериал,
  как в оригинале; кэп 40 для дерева/стали (`InventorySystem.RESOURCE_CAP`).
- `fuel` → `electricity`: НЕ собирается на карте — производится генераторами
  (`generator.gd`, до `MAX_GENERATORS=4` шт., каждый раз в
  `electricity_production_interval` добавляет `electricity_per_tick` в общий
  запас до своего `storage_capacity`). Тир 4 удваивает скорость производства.
  Турели/мортира тратят `electricity_per_shot` за выстрел — без запаса и без
  живого генератора турели простаивают (метка "нет питания", как в 4.14).
- Мастерская: убрана покупка топлива (клавиша L), апгрейд тиров теперь
  считает сталь вместо камня.

✅ 2026-06-15: **4.17 (часть 1) Крафт инструментов — Молот** сделан:
- Мастерская: новая клавиша **M** крафтит Молот (15 дерева/10 стали/50$,
  один раз, `InventorySystem.has_hammer`). С Молотом ремонт построек (**F**)
  восстанавливает **x2 HP** за удар (`player.gd: repair_target`).
- По онлайн-источникам (CS:NZ Shelter, cso.fandom/steamsolo): Молот в
  оригинале крафтится на верстаке и чинит конструкции быстрее обычного топора
  — у нас это реализовано как постоянный множитель ремонта x2.
- Вторая часть 4.17 (Мастерская как ставимая постройка, как турель/генератор)
  — отдельная задача с переработкой сцены `workshop.tscn`, отложена.

🔁 **Пересмотр базового цикла по оригиналу (4.21–4.30)** — решение автора,
2026-06-15…16. Приводим ядро к механике New Zombie Shelter (см. разд. 19).
Нумерация продолжает игровой ЭТАП 4 (чтобы не путать с ЭТАПОМ 5 «ассеты»):
- Деньги — только за оружие; постройки — только за ресурсы.
- Оружие — на чёрном рынке (1 из 3 точек/день).
- Топор — стартовый инструмент: им добываем (1/2/3 за удар по навыку),
  чиним постройки (ЛКМ, бесплатно) и бьём зомби.
- Навыки: 3 очка на старте, +1 за пережитую ночь; ветка «Добыча» 1→2→3.
- Мастерская — UI-меню по клавише E (крафт инструментов по навыкам).
- Боезапаса турелей не будет — убираем механику (турели жмут от генератора).

✅ 2026-06-15: **4.21 Топор как стартовый инструмент + удар ЛКМ** сделан:
- Топор есть с начала игры (`player.axe_equipped`, на старте экипирован).
  ЛКМ с топором → `swing_axe()`: луч из камеры → добыча (узел, 4.22) / урон
  зомби (`axe_damage` 25) / ремонт постройки. Если луч мимо (стены низкие) —
  чиним ближайшую постройку в радиусе.
- **Ремонт теперь бесплатным ударом топора** (без траты дерева, бывшая
  клавиша F убрана; `REPAIR_COST` удалён). Молот по-прежнему даёт x2 HP.
- Топор **без патронов и перезарядки**: R при топоре игнорируется, HUD
  показывает «Топор — ближний бой» вместо счётчика патронов.
- Клавиша **Q** — взять топор; **1-5** — взять ствол (снимает топор).

✅ 2026-06-15: **4.22 Добыча топором** сделана:
- Узлы дерева/стали у базы теперь **бьются топором** (ЛКМ): за удар дают
  `InventorySystem.gather_level` ресурса (база 1, прокачка в 4.23 даёт 1→2→3).
- У узла запас `max_reserve` (10); кончился — узел **истощён** (затемняется),
  восстанавливается до полного **с приходом дня** (`phase_changed`).
- Реализовано флагом `harvestable` в `resource_pickup.gd`: узлы карты бьются
  (слой 4, ловится лучом топора), а дроп с зомби остаётся подбором ходьбой.
  Пули проходят сквозь узлы добычи (`player.shoot` исключает слой 4).

✅ 2026-06-15: **4.23 Навыки (очки + меню N)** сделана:
- `InventorySystem`: `skill_points` (3 на старте), ветки `gather_level`/
  `combat_level`/`engineer_level`, `upgrade_skill()` (1 очко за уровень,
  макс 3), `add_skill_point()`, сигнал `skills_changed`.
- **+1 очко за пережитую ночь**: `day_night_cycle` при переходе ночь→день
  шлёт `EventBus.night_survived`; `InventorySystem` ловит и начисляет очко.
- Эффекты: Добыча → +1 ресурс/удар (4.22); Бой → +10 урона топором; Инженер
  → +5 HP к ремонту (открывает крафт инструментов в 4.26).
- Меню навыков по клавише **N** (`scenes/skill_menu.tscn` + `skill_menu.gd`):
  панель со свободными очками и тремя ветками, кнопки [+] тратят очко;
  на паузе игры (`process_mode = ALWAYS`).

✅ 2026-06-15: **4.24 Чёрный рынок** сделан:
- Новые `scenes/black_market.tscn` + `scripts/black_market.gd` (Area3D-прилавок,
  тёмно-красный). Каждый день рынок открывается в **случайной из 3 точек**
  (`spawn_points`), меняет точку при наступлении дня (`phase_changed`).
- Рядом с рынком за **деньги** покупается оружие ([F], логика/цены в
  `player.gd`). Покупка оружия из **Мастерской убрана** (бывшая клавиша J) —
  деньги теперь тратятся только на рынке.

✅ 2026-06-15: **4.25 Деньги только за ресурсы + питание-мощность** сделано:
- **Боезапас турелей убран целиком**: турели/мортира/гатлинг стреляют без
  расходника. `turret_ammo` удалён из инвентаря/HUD/мастерской; Склад убран
  из меню построек (кормил боезапас).
- **Деньги — только за оружие** (чёрный рынок). Из Мастерской убраны покупка
  стены (G) и лечения (H) за деньги; апгрейд тира — теперь за ресурсы; крафт
  Молота — за ресурсы. Лечение — только через Лазарет.
- **Питание переосмыслено как бюджет мощности** (по уточнению автора): не
  накопление, а «ватты». Генератор даёт фиксированную мощность (40; Тир 4 →
  +20), турель потребляет 30 постоянно. Если суммарной мощности не хватает на
  турель — она простаивает, остальные (что влезли в бюджет) работают. HUD:
  строка «Питание: нагрузка / мощность» (краснеет при нехватке).
- Стоимости тиров снижены под кэп ресурсов (≤40): иначе Тир 3–4 были
  недостижимы (стоили 50/80 дерева при кэпе 40).

✅ 2026-06-15: **4.26 UI-меню построек по клавише B** сделано:
- Новые `scenes/build_menu.tscn` + `build_menu.gd` (CanvasLayer): по **B**
  открывается панель со списком построек (цена в ресурсах + гейт по тиру —
  заблокированные помечены «нужен Тир N»). Клик по постройке → выбор + вход
  в режим постройки (ЛКМ ставит); кнопка «Выйти из режима постройки».
- В `player.gd` B теперь открывает меню (а в режиме постройки — выходит из
  него); прежний цикл по **V** убран. `build_system.get_buildables()` отдаёт
  список для меню.

✅ 2026-06-15: **4.27 Мастерская UI (E) + классовые инструменты** сделано:
- Мастерская открывает **UI-меню по E** (`workshop_menu.tscn`/`.gd`) вместо
  спам-клавиш: стена, апгрейд тира, и крафт классовых инструментов за ресурсы
  (заблокированные помечены 🔒 с требуемой веткой/уровнем навыка).
- **Классовые инструменты** (флаги в `InventorySystem`, гейт по ветке навыка):
  - **Нож** (Бой ур.1): +урон топора, скорость атаки x0.75.
  - **Улучшенный топор** (Добыча ур.2): скорость атаки x0.5 (быстрее всех).
  - **Молот** (Инженер ур.1): ремонт x2 HP + скорость атаки x0.75.
- Добыча за удар по-прежнему ТОЛЬКО от навыка «Добыча»; инструменты её не
  меняют. У топора появился кулдаун удара (`axe_swing_interval`), который
  инструменты ускоряют (`player._axe_swing_interval`).

✅ 2026-06-16: **правки по обратной связи (4.28–4.30)** сделаны:
- **4.28**: все UI-меню (N/B/E) теперь ЗАКРЫВАЮТСЯ своей клавишей/Esc на паузе —
  ловят клавишу опросом в `_process` (виртуал ввода на паузе не доходил).
- **4.29**: добыча — физические удары; у спота случайный запас **5–7 ударов**,
  за удар выдаётся уровень навыка «Добыча» (1/2/3). Реген «на месте» убран —
  споты исчезают и спавнятся СЛУЧАЙНО (`resource_spawner.gd` + `resource_node.tscn`).
- **4.30**: мастерскую и генератор **строит сам игрок** (ставимые постройки),
  на старте их нет; `workshop.gd` переведён на опрос E в `_process`.
- Исправлены краши тестового прогона (рантайм-`Label3D` при `quit()` →
  завершение через `OS.kill`; `Label3D` призрака — `visible=false` вместо
  `queue_free`). На саму игру не влияют. Прогон стабилен (8/8 EXIT=0).

✅ 2026-06-16: **правки по обратной связи (4.31)** сделаны:
- Паузы в игре НЕТ — меню не останавливают игру (только курсор); Esc закрывает.
- Починен баг урона по зомби: зомби и узлы добычи были на одном слое (4) →
  пули проходили сквозь зомби. Узлы добычи → слой 16, `shoot` исключает 16.
- Ресурсы спавнятся ВНЕ периметра базы и не на стенах/постройках (оверлап-чек);
  число спотов случайное (дерево 3–5, сталь 2–4).
- Тексты (HP/подсказки) не видны сквозь стены и издалека (`visibility_range_end`).
- Нельзя строить постройку на постройку; нехватка ресурсов гасит кнопку в меню.

✅ 2026-06-16: **правки по обратной связи (4.32)** сделаны:
- Споты ресурсов больше НЕ заменяются мгновенно 1-в-1 после сбора. Число узлов
  на карте НЕ фиксировано: исчерпанный узел исчезает, а новые «дозревают» в
  СЛУЧАЙНЫЕ моменты по таймеру (`resource_spawner.respawn_min..respawn_max`,
  15–30 c) и только пока тип под лимитом (`wood_max`/`steel_max`) — залежей не
  слишком много. `_on_node_depleted` больше не спавнит замену; добавлены
  `_on_respawn_tick`/`_arm_respawn_timer`/`_count_of`.
- Зажатая ЛКМ с ближним инструментом — НЕПРЕРЫВНАЯ добыча/ремонт/бой, но НЕ
  моментально: удар → кулдаун → удар (`player._physics_process` опрашивает
  `Input.is_mouse_button_pressed` и зовёт `swing_axe`, темп задаёт `_swing_timer`).
  Кулдаун зависит от инструмента (`_axe_swing_interval`, этап 4.27): улучшенный
  топор бьёт быстрее всех (×0.5), нож/молот — ×0.75, базовый топор — ×1.0.
  Инструменты в игре — флаги-апгрейды (`has_improved_axe`/`has_hammer`/`has_knife`)
  поверх топора, поэтому зажатие при `axe_equipped` покрывает их все. Для стволов
  авто-огня нет (выстрел — по одиночному клику).
- Баланс ближнего боя: бонус ветки «Бой» снижен с ×10 до ×4 за уровень
  (`player.swing_axe`). Было: бой 3 → +30 (итог 55, с ножом 65, сносил танка 80
  за 2 удара); стало: бой 3 → +12 (итог 37). База топора 25 не менялась.
- Проверено через capture: старт — случайное число узлов вне базы; истощение
  «было 8 → стало 7 (без мгновенной замены)»; «тик спавнера: 7 → 8» (дозрев по
  таймеру, под лимитом); «ближний урон при бое 3: 37 (раньше 55)»; выстрел сквозь
  узел — HP зомби 30→20; EXIT=0, ошибок парсера нет.

🎉 **Пересмотр базового цикла по оригиналу (4.21–4.32) завершён.**
Следующее — по разд. 17/18 (тех-постройки/sanity, опционально) или ЭТАП 5
(ассеты/скины). Отложенное из разд. 18 (4.17 ч.2, 4.18) поглощено серией.

---

### Классы (4.12) + боссы/спецволны/C4 (4.13) — по методологии superpowers
Дизайн-спек: [docs/superpowers/specs/2026-06-16-classes-bosses-special-waves-design.md].
Под-этапы: 4.12a классы+UI → 4.12b способности+C4 → 4.13a спецзомби → 4.13b босс+спецволны.

✅ 2026-06-17: **4.12a — классы игрока + навыки + UI** сделано:
- В `InventorySystem`: `player_class` (combat/gather/engineer), сигнал `class_changed`,
  потолок чужой ветки = база+1 (`SKILL_BASE`/`OFF_CLASS_EXTRA`, своя — до `SKILL_MAX`),
  `set_class`/`unlock_ability`/`ability_unlocked`/`get_skill_cap`/`get_resource_cap`/
  `reset_run_progression`; флаги способностей `has_airstrike`/`has_campfire`/`has_c4`,
  `c4_charges` (эффекты — в 4.12b).
- Стат-эффекты класса: Боец +15 макс HP/ур. (`player._apply_combat_hp`), Добытчик
  +20 лимит ресурсов/ур. (`InventorySystem.get_resource_cap`, влияет на `add_resource`),
  Инженер +10% урон турелей/ур. (`turret._try_fire`).
- Экран выбора класса на старте забега (`scenes/class_select.tscn`/`class_select.gd`,
  в `main.tscn`); в capture скрыт, класс задаёт тест. Меню навыков N (`skill_menu.gd`)
  показывает класс, потолки веток и строку сигнатурной способности.
- Проверено через capture (секция 4.12a): класс=combat; Бой 3/3 + потолок; макс HP
  100→145; чужая «Добыча» 1-е true/2-е false (ур 2/2); Боец→Авиаудар (C4=false);
  Добытчик лимит 60→100; Инженер турель ×1.2 и C4=true; EXIT=0.

✅ 2026-06-17: **4.12b — сигнатурные способности классов (вкл. C4)** сделано:
- Клавиша **F** → `player.use_class_ability()` ветвится по классу/открытой способности:
  Авиаудар (Боец) — AoE по точке прицела с задержкой + кулдаун; Костёр (Добытчик) —
  `campfire.tscn`, лечит игрока рядом, один одновременно; C4 (Инженер) — `c4.tscn`,
  таймер→взрыв: AoE по зомби и снос ТОЛЬКО сегментов группы `blastable`
  (`blastable_segment.tscn`), обычные постройки/база НЕ разрушаются.
- Крафт C4 за ресурсы в мастерской (`workshop.craft_c4`, кнопка в `workshop_menu`,
  только при `has_c4`); счётчик зарядов `InventorySystem.c4_charges`.
- HUD: индикатор способности/кулдауна/зарядов (`hud.AbilityLabel`, `_update_ability`).
- Один сегмент `blastable` размещён в `main.tscn` (демонстрация прохода).
- Проверено через capture (4.12b): Авиаудар уничтожил зомби + кулдаун; Костёр
  HP 60→63.6; крафт C4 0→1; взрыв C4 — зомби уничтожен, сегмент снесён, постройка
  цела (true/true/true); EXIT=0.

🎉 **4.12 (классы игрока + дерево навыков) завершён.** Дальше — 4.13 (спецзомби,
босс «Колосс», спецволны, HP-бар).

✅ 2026-06-17: **4.13a — спецзомби (Крикун + Взрывной)** сделано:
- **Крикун** (`screamer.gd`/`screamer.tscn`, наследует `zombie.gd`): при первом
  обнаружении игрока в радиусе один раз «кричит» → `wave_manager.summon_extra(n)`
  доспавнивает группу зомби; алерт HUD через `EventBus.screamer_called`.
- **Взрывной** (`exploder.gd`/`exploder.tscn`): расширяет `_on_died` — при смерти
  AoE урон игроку и постройкам (группа `building`) в радиусе, затем штатная смерть.
- `EventBus`: добавлен `screamer_called` (+ заранее `boss_spawned`/`boss_health_changed`/
  `boss_defeated`/`special_wave` под 4.13b).
- Проверено через capture (4.13a): Крикун кричал=true, доспавнил 3 (enemy=4);
  Взрывной при смерти — HP игрока 100→70, HP стены 42→12; EXIT=0.

---

## ЭТАП 0 — Подготовка и среда 🧱
- [~] **0.1 Установка инструментов**
  - [x] 0.1.1 Godot 4 (установлен в `D:\Godot`, v4.6.3)
  - [ ] 0.1.2 Blender
  - [ ] 0.1.3 Git + удалённый репозиторий (есть `.gitignore`, но репозиторий не инициализирован)
  - [~] 0.1.4 VS Code (+ расширение «Godot Tools» — проверить)
  - [ ] 0.1.5 Регистрация в ИИ-сервисах (Meshy/Tripo, Mixamo, Suno)
- [~] **0.2 Изучение базы Godot** *(идёт по ходу проекта)*
  - [~] 0.2.1 Официальный туториал «Your first 3D game»
  - [~] 0.2.2 Ноды → сцены → инстансы
  - [~] 0.2.3 Сигналы (signals)
  - [~] 0.2.4 Основы GDScript
  - [ ] 0.2.5 Ресурсы (`Resource`) и `@export`
- [~] **0.3 Организация проекта**
  - [x] 0.3.1 Структура папок (`scenes/`, `scripts/`, `docs/`)
  - [x] 0.3.2 Конвенции именования
  - [x] 0.3.3 `.gitignore` для Godot
  - [ ] 0.3.4 Регулярные коммиты + бэкап

## ЭТАП 1 — Проектирование игры (GDD) 🧱
- [~] **1.1 Концепция и видение** *(обсуждено устно, не задокументировано)*
  - [~] 1.1.1 Питч в одно предложение
  - [ ] 1.1.2 Референсы и целевая аудитория
  - [ ] 1.1.3 Уникальная фишка (USP)
- [~] **1.2 Основной игровой цикл** *(описан в ROADMAP)*
  - [~] 1.2.1 Петля собрать→скрафтить→построить→оборонять→прокачать
  - [ ] 1.2.2 Действия минута-в-минуту
  - [ ] 1.2.3 Действия за сессию / за всю игру
- [ ] **1.3 Детальное описание механик**
  - [~] 1.3.1 Перемещение и управление *(реализовано в коде, не описано)*
  - [~] 1.3.2 Боевая система *(базовая стрельба есть)*
  - [ ] 1.3.3 Сбор ресурсов
  - [ ] 1.3.4 Крафт
  - [ ] 1.3.5 Строительство базы
  - [ ] 1.3.6 Враги и волны
  - [ ] 1.3.7 Прогрессия и апгрейды
- [ ] **1.4 Контент-план (списки)**
  - [ ] 1.4.1 Ресурсы
  - [ ] 1.4.2 Предметы и оружие
  - [ ] 1.4.3 Постройки
  - [ ] 1.4.4 Враги
  - [ ] 1.4.5 Карты / арены
- [ ] **1.5 Экономика и баланс «на бумаге»**
  - [ ] 1.5.1 Стоимости крафта
  - [ ] 1.5.2 Кривая сложности волн
  - [ ] 1.5.3 Темп прогрессии
- [ ] **1.6 Финализация документа**
  - [ ] 1.6.1 Записать GDD
  - [ ] 1.6.2 Разделить scope MVP и полной игры
  - [ ] 1.6.3 Зафиксировать anti-scope («что НЕ делаем»)

## ЭТАП 2 — Технический фундамент / архитектура 🧱
- [~] **2.1 Архитектура проекта**
  - [x] 2.1.1 Структура главной сцены (`main.tscn`)
  - [ ] 2.1.2 Автозагрузки (Autoload): `GameManager`, `EventBus`
  - [ ] 2.1.3 Шина событий (сигналы)
  - [ ] 2.1.4 Данные через `Resource` (`ItemData`, `EnemyData`…)
- [x] **2.2 Базовый игрок**
  - [x] 2.2.1 `CharacterBody3D` + коллизия
  - [x] 2.2.2 Скрипт движения (WASD, гравитация, прыжок)
  - [x] 2.2.3 Камера от первого лица
  - [ ] 2.2.4 Карта ввода (Input Map) *(сейчас прямое чтение клавиш — мигрируем позже)*
- [~] **2.3 Базовый мир**
  - [x] 2.3.1 Тестовый уровень (земля, свет, небо)
  - [x] 2.3.2 Коллизии окружения
  - [ ] 2.3.3 Навигационная сетка `NavigationRegion3D` *(отложено — арена плоская, зомби идёт напрямую)*
- [~] **2.4 Переиспользуемые системы-основы**
  - [x] 2.4.1 `HealthComponent`
  - [~] 2.4.2 Система урона *(урон через raycast+take_damage есть; hitbox/hurtbox-зоны — нет)*
  - [ ] 2.4.3 Машина состояний игры (`prep→wave→pause→game_over`)

## ЭТАП 3 — Прототип основного цикла (MVP) 🎯
- [x] **3.1 Боёвка-минимум**
  - [x] 3.1.1 Оружие (рейкаст)
  - [x] 3.1.2 Стрельба / патроны / перезарядка
  - [x] 3.1.3 Нанесение урона цели *(работает на мишенях)*
- [x] **3.2 Враг-минимум**
  - [x] 3.2.1 Зомби идёт к игроку (напрямую; NavMesh отложен — арена плоская)
  - [x] 3.2.2 Поведение: преследование → атака → смерть
  - [x] 3.2.3 Атака и урон игроку
  - [x] 3.2.4 Смерть и дроп ресурса (4.7.1)
- [~] **3.3 Спавн волн**
  - [x] 3.3.1 `WaveManager` (кол-во, тайминг, нарастание сложности)
  - [x] 3.3.2 Точки спавна (6 Marker3D по краям арены)
  - [~] 3.3.3 Старт волны и подсчёт работают; зачистка→следующая волна реализована (по `tree_exited`), в прогоне ещё не проверена
- [x] **3.4 Ресурсы и крафт-минимум**
  - [x] 3.4.1 Ресурсная нода (Area3D с дробом при контакте)
  - [x] 3.4.2 Простой инвентарь (dict: wood/stone/wall, автозагрузка)
  - [x] 3.4.3 Один рецепт крафта (2 дерева → 1 стена)
- [x] **3.5 Строительство-минимум**
  - [x] 3.5.1 Режим постройки с превью-«призраком»
  - [x] 3.5.2 Привязка к сетке и размещение
  - [x] 3.5.3 Стена с `HealthComponent` *(турель — отложена до этапа 4)*
- [x] **3.6 Цикл день/ночь**
  - [x] 3.6.1 Таймер фаз
  - [x] 3.6.2 Переключение освещения
  - [x] 3.6.3 Запуск волны ночью
- [x] **3.7 Состояния игры**
  - [x] 3.7.1 Победа (пережить N волн)
  - [x] 3.7.2 Поражение (игрок убит)
  - [x] 3.7.3 Экраны итога + рестарт (клавиша R)
- [ ] **3.8 Первый внутренний плейтест**
  - [ ] 3.8.1 Пройти цикл от начала до конца
  - [ ] 3.8.2 Записать: весело / скучно / сломано

## ЭТАП 4 — Расширение игровых систем 🔁
> Приоритеты пересмотрены по разбору режима «Shelter»
> (см. [CS_NZ_SHELTER_ANALYSIS.md](CS_NZ_SHELTER_ANALYSIS.md), разделы 15-16).
> 4.1-4.5 — ядро «ощущения Shelter», делать первыми и по порядку.
- [x] **4.1 Структура убежища с проёмами** ⚠️ *(главный пункт этапа)*
  - [x] 4.1.1 Периметр вокруг базы (вместо открытой арены)
  - [x] 4.1.2 1-3 явных проёма для баррикад
  - [x] 4.1.3 Точки спавна зомби снаружи периметра, путь к проёмам
- [x] **4.2 Приоритет атаки зомби: постройки vs игрок**
  - [x] 4.2.1 Выбор цели — ближайший доступный проём/баррикада или игрок
  - [x] 4.2.2 Атака баррикады, если она на пути
- [x] **4.3 Ремонт баррикад**
  - [x] 4.3.1 Действие «ремонт» (тратит ресурсы, восстанавливает HP)
  - [x] 4.3.2 Индикация HP баррикады
- [x] **4.4 Второй тип зомби — танк**
  - [x] 4.4.1 Больше HP, медленнее, сильнее бьёт по постройкам
  - [x] 4.4.2 Приоритизация баррикад выше обычного зомби
  - [x] 4.4.3 Простое визуальное отличие
- [x] **4.5 Нарастание сложности между ночами**
  - [x] 4.5.1 Масштабирование волн по номеру ночи
  - [x] 4.5.2 Доля танков растёт с ночами
- [x] **4.6 Боевая система — развитие**
  - [x] 4.6.1 Патроны и перезарядка (3.1.2)
  - [x] 4.6.2 Несколько видов оружия
- [x] **4.7 Экономика и крафт — развитие**
  - [x] 4.7.1 Дроп ресурсов с зомби (3.2.4)
  - [x] 4.7.2 Двойная экономика (ресурсы + деньги)
  - [x] 4.7.3 Точка крафта/мастерской
- [~] **4.8 Специализированные постройки**
  - [x] 4.8.1 Турель + расходник Turret Component (аналог Gatling)
  - [x] 4.8.2 Лазарет, склад
  - [x] 4.8.3 Мортирная турель (Mortar) — навесной огонь по площади, бьёт из-за стен
  - [x] 4.8.4 Разнообразие турелей: дешёвая ранняя башня (Gun Tower) vs дорогая Gatling
- [x] **4.9 HUD — индикаторы угроз**
  - [x] 4.9.1 «Проём/баррикада под атакой»
  - [x] 4.9.2 Оповещения о волнах/брешах
- [x] **4.10 Juggernaut-подобный враг** (мини-босс, ломает постройки)
  - [x] 4.10.1 Большое HP, целится в постройки, тяжёлый урон по баррикадам
  - [x] 4.10.2 Аггро на игрока вблизи (риск/выгода) + награда + HUD-оповещения
- [x] **4.11 Эвакуация как условие победы** (развитие 3.7.1)
  - [x] 4.11.1 После N волн вместо мгновенной победы вызывается транспорт + зона эвакуации
  - [x] 4.11.2 Добежать до зоны за отсчёт → победа; не успел → поражение; HUD-оповещение и таймер
- [x] 4.12 Классы игрока + дерево навыков (брейнсторм+спек superpowers, 2026-06-16)
  - [x] 4.12a классы (combat/gather/engineer) + потолки веток + стат-эффекты + экран выбора + меню N, 2026-06-17
  - [x] 4.12b сигнатурные способности (F): Авиаудар/Костёр/C4 + крафт C4 + сегменты blastable, 2026-06-17
- [~] 4.13 Боссы и спец-волны / C4
  - [x] 4.13a спецзомби: Крикун (доспавн) + Взрывной (AoE при смерти), 2026-06-17
  - [ ] 4.13b босс «Колосс» (слэм) + теги спецволн (босс-ночь/орда/подмешивание) + HP-бар + алерты HUD

> Углубление обороны/базы из каталога построек (CS_NZ_SHELTER_ANALYSIS.md разд. 17).
> Крупные системы оригинала — НЕ MVP, делать после плейтеста 3.8 и только если цикл весел.
- [x] 4.14 Система питания убежища (генератор + электричество для турелей)
- [x] 4.15 Тиры убежища (1→4) (прокачка базы через мастерскую)
- [x] 4.16 Переработка ресурсов (камень→сталь, fuel→электричество от генераторов, разд. 18)
- [~] 4.17 Мастерская как постройка + крафт инструментов ⏳ (разд. 18.5) — крафт Молота сделан (2026-06-15), постройка-версия мастерской в работе
- [ ] 4.18 UI-меню построек по клавише B ⏳ (разд. 18.4)
- [ ] 4.19 Технические постройки ⏳ (Comm Tower, Refinery — завязаны на классы 4.12)
- [ ] 4.20 Campfire + «рассудок» (sanity) ⏳ (самый низкий приоритет)

### Пересмотр базового цикла по оригиналу (разд. 19)
- [x] 4.21 Топор — стартовый инструмент + удар ЛКМ (добыча/ремонт/ближний бой), 2026-06-15
- [x] 4.22 Добыча топором: узлы-удары, выход 1/2/3 по навыку, дневной реген, 2026-06-15
- [x] 4.23 Навыки: очки (3 на старте, +1 за пережитую ночь) + меню N, 2026-06-15
- [x] 4.24 Чёрный рынок: оружие за деньги в 1 из 3 точек/день, 2026-06-15
- [x] 4.25 Деньги только за ресурсы + удаление боезапаса + питание-мощность, 2026-06-15
- [x] 4.26 UI-меню построек по клавише B (как в оригинале; заменил toggle+V; бывш. 4.18), 2026-06-15
- [x] 4.27 Мастерская: UI-меню (E) + крафт классовых инструментов (нож/улучш.топор/молот), 2026-06-15
- [x] 4.28 Все UI-меню закрываются своей клавишей/Esc на паузе (опрос в `_process`), 2026-06-16
- [x] 4.29 Добыча — физические удары: у спота случайный запас 5–7 ударов, выход = навык за удар; споты спавнятся СЛУЧАЙНО (без регена на месте), 2026-06-16
- [x] 4.30 Мастерскую и генератор строит сам игрок (постройки, не предустановлены), 2026-06-16
- [x] 4.31 Без пауз в меню; баг урона по зомби (слои); спавн ресурсов вне базы + рандом кол-во; тексты не сквозь стены/издалека; нельзя строить на постройку; гейт ресурсов в меню, 2026-06-16
- [x] 4.32 Споты не заменяются мгновенно (дозрев по таймеру с лимитом); зажатая ЛКМ — непрерывная добыча/ремонт/бой с кулдауном (улучш. топор быстрее); баланс «Бой» ×10→×4, 2026-06-16

## ЭТАП 5 — Производство контента (ассеты через ИИ) 🤖
- [ ] 5.1 Пайплайн 3D-моделей (генерация → Blender → glTF → Godot)
- [ ] 5.2 Персонажи и анимация (Mixamo: риг + анимации)
- [ ] 5.3 Окружение и пропсы
- [ ] 5.4 Текстуры и материалы (PBR)
- [ ] 5.5 VFX / партиклы
- [ ] 5.6 Единый арт-стиль

## ЭТАП 6 — Интерфейс (UI/UX) 🔁
- [~] 6.1 HUD *(есть прицел, HP, инвентарь, фаза дня/ночи; патроны — нет)*
- [ ] 6.2 Меню (главное, пауза, настройки)
- [ ] 6.3 Экраны систем (инвентарь, крафт, строительство)
- [ ] 6.4 Обратная связь игроку (индикаторы урона, оповещения)
- [ ] 6.5 Туториал / онбординг
- [ ] 6.6 Локализация (рус/англ)

## ЭТАП 7 — Аудио 🤖
- [ ] 7.1 Музыка (меню / день / ночь-бой)
- [ ] 7.2 SFX (выстрелы, попадания, зомби, UI)
- [ ] 7.3 Звуковой движок (`AudioStreamPlayer3D`, шины)
- [ ] 7.4 Микс и динамика по фазам

## ЭТАП 8 — Сохранения и прогрессия
- [ ] 8.1 Система сохранения/загрузки
- [ ] 8.2 Сохранение состояния (база, инвентарь, прогресс)
- [ ] 8.3 Настройки (графика, звук, управление)
- [ ] 8.4 Мета-прогрессия между забегами

## ЭТАП 9 — Баланс и геймдизайн-итерации 🔁
- [ ] 9.1 Кривая сложности волн
- [ ] 9.2 Баланс экономики
- [ ] 9.3 Баланс оружия и врагов
- [ ] 9.4 Темп игры (pacing)
- [ ] 9.5 Итерации по фидбеку

## ЭТАП 10 — Полировка (game feel)
- [ ] 10.1 Game feel (отдача, тряска камеры, хитстоп)
- [ ] 10.2 Партиклы и пост-обработка
- [ ] 10.3 Анимации переходов UI
- [ ] 10.4 Освещение и атмосфера
- [ ] 10.5 Тактильный фидбек

## ЭТАП 11 — Тестирование и оптимизация
- [ ] 11.1 Плейтесты с другими людьми
- [ ] 11.2 Багфиксинг (трекер багов)
- [ ] 11.3 Профилирование
- [ ] 11.4 Оптимизация (пулинг объектов, LOD, occlusion)
- [ ] 11.5 Тест на целевом железе

## ЭТАП 12 — Мультиплеер / кооп ⚠️ (поздняя фаза)
- [ ] 12.1 Выбор стека (Godot High-Level Multiplayer / ENet / Steam)
- [ ] 12.2 Архитектура authority
- [ ] 12.3 Синхронизация (`MultiplayerSynchronizer`, `MultiplayerSpawner`)
- [ ] 12.4 Сетевой ввод и сглаживание
- [ ] 12.5 Тест по сети, лаг-компенсация

## ЭТАП 13 — Сборка и релиз
- [ ] 13.1 Экспорт-шаблоны (Windows / Linux / Web)
- [ ] 13.2 Иконка, метаданные, версия
- [ ] 13.3 Страница на itch.io
- [ ] 13.4 Маркетинг-минимум (девлоги, соцсети)
- [ ] 13.5 Релиз (демо / ранний доступ)

## ЭТАП 14 — Пострелиз и масштабирование
- [ ] 14.1 Сбор фидбека игроков
- [ ] 14.2 Патчи и хотфиксы
- [ ] 14.3 Новый контент (карты, враги, режимы)
- [ ] 14.4 Решение по Steam / монетизации
- [ ] 14.5 Сообщество (Discord)
- [ ] 14.6 Долгосрочная дорожная карта

---

### 📋 Журнал изменений
- **2026-06-07** — создан трекер. Готово: установка Godot, структура проекта, игрок (движение + камера от 1-го лица), `HealthComponent`, стрельба рейкастом, мишени, прицел.
- **2026-06-11** — игрок получил здоровье (100 HP); добавлен зомби (преследование/атака/смерть), стреляется и атакует игрока; мишени заменены зомби; режим прогона расширен (выстрелы + дамп состояния) и теперь игнорирует реальный ввод.
- **2026-06-11** — добавлен `WaveManager` (волны зомби из точек спавна, нарастание сложности); игрок и зомби разведены по слоям столкновений (зомби сталкиваются только с землёй) — устранён физический «взрыв» тел при контакте.
- **2026-06-12** — добавлена система ресурсов и крафта (3.4): `InventorySystem` (автозагрузка), `ResourcePickup` (Area3D для сбора), `CraftSystem` (1 рецепт: дерево→стена), HUD показывает инвентарь; тестировано через capture режим.
- **2026-06-12** — HUD показывает HP игрока; добавлено строительство-минимум (3.5): `BuildSystem` (режим постройки клавишей B, превью-призрак, привязка к сетке 1x1, размещение по ЛКМ за 1 стену), `wall.tscn`/`wall.gd` со `HealthComponent` (50 HP); тестировано через capture режим.
- **2026-06-12** — добавлена клавиша C для крафта стены (раньше крафт был доступен только в автотесте).
- **2026-06-12** — добавлен цикл день/ночь (3.6): `DayNightCycle` (таймер фаз, плавная смена освещения через `DirectionalLight3D` и `Environment.ambient_light_*`), HUD показывает текущую фазу и оставшееся время; `WaveManager` больше не стартует волны сам — `start_wave()` вызывается ночью. Заодно исправлена ошибка перезапуска сцены при смерти игрока (зомби обращались к уже освобождённым узлам — `reload_current_scene` теперь вызывается отложенно).
- **2026-06-12** — добавлены состояния игры (3.7): `GameStateManager` следит за смертью игрока (поражение) и зачисткой 5-й волны (победа); `WaveManager` теперь сигналит `wave_cleared`. При исходе игра ставится на паузу (`get_tree().paused`), HUD показывает экран "ПОБЕДА!"/"ПОРАЖЕНИЕ" с подсказкой рестарта, клавиша R перезапускает сцену. Игрок при смерти больше не перезапускает сцену сам.
- **2026-06-13** — сделан полный разбор оригинального режима «Shelter» (CS Nexon Zombies), включая веб-поиск: `docs/CS_NZ_SHELTER_ANALYSIS.md` (16 разделов + таблица сопоставления). На основе разбора пересмотрен и переупорядочен ЭТАП 4 в `ROADMAP.md` и `PROGRESS.md`: ядро — 4.1 убежище с проёмами, 4.2 приоритет атаки зомби на постройки, 4.3 ремонт баррикад, 4.4 зомби-танк, 4.5 нарастание сложности между ночами; остальное (экономика, спец-постройки, Juggernaut, эвакуация, классы) — после ядра.
- **2026-06-13** — реализован Этап 4.1 (структура убежища с проёмами): арена `main.tscn` теперь огорожена периметром из 7 статичных сегментов стен (запад — сплошной, север/восток/юг — с проёмами), 3 проёма помечены оранжевыми маркерами на земле. 6 точек спавна зомби перенесены наружу периметра так, что прямая линия до игрока проходит через ближайший проём — зомби «естественно» идут через проёмы без NavMesh. Проверено через capture-режим.
- **2026-06-13** — реализован Этап 4.2 (приоритет атаки зомби: постройки vs игрок): `zombie.gd` теперь каждый кадр ищет ближайшую постройку (группа `building`) в радиусе `building_attack_range`; если она найдена — зомби останавливается и бьёт по ней вместо игрока. Поскольку зомби физически идёт напрямую к игроку и упирается в стену/баррикаду на пути (коллизии слой 1), это естественно даёт приоритет атаки построек, блокирующих проход. Проверено через capture-режим (без ошибок).
- **2026-06-13** — реализован Этап 4.3 (ремонт баррикад): клавиша R ремонтирует ближайшую постройку в радиусе `repair_range` (4 м) за 1 дерево, восстанавливая 15 HP (`player.gd: repair_target()`); ремонт ищет постройку по дистанции, а не лучом камеры — стены низкие (1 м), а камера на высоте 1.6 м, горизонтальный луч по ним не попадает. `wall.tscn`/`wall.gd` получили `Label3D` с текущим HP над стеной (зелёный/жёлтый/красный по доле здоровья), обновляется через сигнал `health_changed`. Проверено через capture-режим: стена 50→30 HP (урон) →45 HP (ремонт, -1 дерево).
- **2026-06-13** — реализован Этап 4.4 (второй тип зомби — танк): `zombie.gd` получил отдельный параметр `building_attack_damage` (урон по постройкам, раньше совпадал с уроном по игроку). Новая сцена `scenes/zombie_tank.tscn` использует тот же скрипт с другими экспортами: HP 80 (вместо 30), скорость 1.3 (вместо 2.5), урон по игроку 14, урон по постройкам 20, радиус атаки построек 3.0 (вместо 2.0) — танк раньше переключается на баррикаду. Визуально — крупнее (капсула радиус 0.75/высота 2.6) и тёмно-красный. `WaveManager` получил `tank_zombie_scene`/`tank_starting_wave`/`tanks_per_wave` — танки подключаются с волны 2. Проверено через capture-режим: вручную заспавненный танк имеет 80 HP, виден на скриншоте крупнее и темнее обычного зомби.
- **2026-06-13** — реализован Этап 4.5 (нарастание сложности между ночами): количество обычных зомби в волне уже росло линейно (`first_wave_count + (волна-1) * count_increment`, с 3.3) — это и есть масштабирование по номеру ночи (4.5.1), дополнительно добавлено сокращение паузы между спавнами с каждой следующей ночью (`spawn_interval - (волна-1) * spawn_interval_decrease`, не ниже `min_spawn_interval=0.15`), так что зомби на поздних ночах появляются плотнее. Для танков (4.5.2) добавлен `tank_wave_step=2`: количество танков = `tanks_per_wave + (волна - tank_starting_wave) / tank_wave_step`, т.е. начиная с волны 2 — 1 танк, с волны 4 — 2, с волны 6 — 3 и т.д. Проверено через capture-режим: волна 1 → "зомби: 3, танков: 0, интервал спавна: 0.5" (без изменений на первой ночи, как и должно быть).
- **2026-06-14** — реализован Этап 4.6.1 (патроны и перезарядка): у игрока теперь обойма (`magazine_size=8`), выстрел расходует патрон и блокируется при пустой обойме («патроны закончились — нажмите R для перезарядки»); клавиша **R** перезаряжает обойму за `reload_time=1.5` с (асинхронно, повторный вызов во время перезарядки игнорируется), клавиша ремонта построек перенесена с R на **F**. HUD получил строку «Патроны: X / Y» (`AmmoLabel`), обновляется через сигнал `ammo_changed`. Проверено через capture-режим: расстрел обоймы 8→0, попытка выстрела при 0 блокируется, после `reload()` патроны восстанавливаются до 8/8 — видно и в консоли, и на HUD скриншота.
- **2026-06-14** — реализован Этап 4.6.2 (несколько видов оружия): у игрока теперь массив `weapons` с двумя оружиями — «Пистолет» (10 урона, обойма 8, перезарядка 1.5с, дальность 100м, 1 пуля без разброса) и «Дробовик» (6 урона за пулю, 5 пуль с разбросом за выстрел, обойма 4, перезарядка 2.2с, дальность 20м); переключение клавишами **1**/**2** (`switch_weapon()`), у каждого оружия отдельно хранятся патроны в обойме, переключение блокируется во время перезарядки. HUD строка объединена в «Оружие — Патроны: X / Y» (`AmmoLabel`, сигнал `weapon_changed`). Проверено через capture-режим: переключение на дробовик меняет урон/обойму/дальность, выстрел дробовика попадает 4/5 пуль и тратит 1 патрон из своей обоймы (3/4), переключение обратно на пистолет восстанавливает его обойму 8/8.
- **2026-06-14** — реализован Этап 4.7.1 (дроп ресурсов с зомби, закрывает TODO из 3.2.4). Камень теперь добывается так же, как дерево — на карте появились 2 точки `StonePickup` (`scenes/main.tscn`, по 10 камня), `resource_pickup.gd` окрашивает дерево/камень разными цветами (коричневый/серый). Дроп с зомби — редкий случайный бонус: `zombie.gd` при смерти с шансом `drop_chance` (0.25, у танка 0.5) создаёт `ResourcePickup` случайного типа (`drop_resource_types`) и количества (`drop_amount_min`..`drop_amount_max`). Проверено через capture-режим: из 6 убитых танков выпало 2 дропа («wood x2», «stone x3») — соответствует шансу 0.5.
- **2026-06-14** — реализован Этап 4.7.2 (двойная экономика: ресурсы + деньги). В `InventorySystem` добавлена вторая валюта `money` (отдельно от инвентаря ресурсов) с сигналом `money_changed` и методами `add_money`/`spend_money`/`get_money`. Зомби при смерти начисляют деньги (`zombie.gd: money_reward`, обычный — 10, танк — 30, задано в `zombie_tank.tscn`). HUD получил строку «Деньги: N$» (`MoneyLabel`, сигнал `money_changed`). Разделение: дерево/камень → крафт и постройка, деньги → покупки в мастерской. Проверено через capture-режим: после убийства 6 танков на счету 190$ (10 + 6×30), HUD показывает баланс.
- **2026-06-14** — реализован Этап 4.7.3 (точка крафта/мастерская). Новые `scenes/workshop.tscn` + `scripts/workshop.gd` — станция-`Area3D` (оранжевый верстак с подсказкой-`Label3D`) в убежище (`main.tscn`, позиция (-3,0,3)). Игрок должен стоять рядом (Area3D детектит игрока по слою 2), чтобы: **C** — скрафтить стену из ресурсов (2 дерева → 1 стена), **G** — купить стену за деньги (25$), **H** — купить лечение за деньги (+25 HP за 30$). Глобальная клавиша крафта **C** убрана из `player.gd` — крафт теперь только у верстака; игроку добавлены методы `heal()`/`is_full_health()`. Так замкнуты обе ветки экономики. Проверено через capture-режим: крафт стены (дерево 12→10, +1 стена), покупка стены (190→165$, +1 стена), покупка лечения (HP 60→85, 165→135$), «игрок в зоне мастерской: true» при подходе вплотную; мёртвому лечение не продаётся.
- **2026-06-14** — деньги теперь тратятся на оружие (развитие 4.7.2). В начале игры у игрока **только пистолет** (`player.gd: _owned`, стартует куплен лишь индекс 0); каждое оружие получило цену (`weapons[*].price`: пистолет 0, дробовик 50). Переключение на некупленное оружие (`switch_weapon`) отклоняется; добавлены `buy_weapon(index)` (тратит деньги, помечает купленным и сразу делает активным) и `owns_weapon(index)`. В мастерской добавлена клавиша **J** — купить оружие (`workshop.gd: buy_weapon()` → `player.buy_weapon`); подсказка верстака показывает название/цену оружия или «куплено». Проверено через capture-режим: до покупки переключение на дробовик отклонено (остаётся пистолет), покупка за 50$ (деньги 60→10$) делает дробовик активным, он стреляет (4/5), деньги списываются.
- **2026-06-14** — реализован Этап 4.8.2 (лазарет + склад). Новые `scenes/infirmary.tscn`+`scripts/infirmary.gd` (**Лазарет** — белый блок с красным крестом): лечит игрока, пока он в радиусе `heal_range` (5 м), со скоростью `heal_rate` (8 HP/с) — бесплатно и пассивно, в отличие от платного лечения в мастерской. Новые `scenes/storage.tscn`+`scripts/storage.gd` (**Склад боеприпасов** — коричневый ящик): каждые `supply_interval` (2 с) добавляет `supply_amount` (1) общего боезапаса турелей — пассивно кормит оборону (синергия с 4.8.1). Обе постройки в группе `building` (HP 50, зомби ломают, игрок чинит на F) и добавлены в систему постройки (`build_system.gd`, выбор по клавише **V**): лазарет — 3 дерева + 3 камня, склад — 4 дерева + 1 камень; добавлен `select_buildable(name)` для выбора здания. Проверено через capture-режим: лазарет построен и вылечил игрока (HP 45→57 за 1.5 с, далее до 95 у стоящего рядом), склад построен и пополнил боезапас (6→8 за два тика).
- **2026-06-14** — реализован Этап 4.8.1 (турель + расходник). Новые `scenes/turret.tscn` + `scripts/turret.gd` — авто-турель (синее основание + вращающийся ствол): сама наводится на ближайшего зомби в радиусе `fire_range` (12 м) и стреляет по кулдауну (`fire_interval` 0.8 с, урон 12), расходуя общий боезапас турелей (`InventorySystem` ресурс `turret_ammo`) — это её «расходник» (Turret Component); без боезапаса простаивает. Как стена, входит в группу `building` (зомби её ломают, игрок чинит на F), HP 60. Система постройки обобщена на несколько типов (`build_system.gd`): теперь хранит список построек со своей ценой (стена — 1 «wall»; турель — 3 камня + 2 дерева), клавиша **V** переключает тип в режиме стройки, призрак-превью пересобирается. Мастерская продаёт боезапас турелей (клавиша **K**: +10 за 20$). HUD переведён на русские названия ресурсов (Дерево/Камень/Стена/Патроны турелей) и метка денег опущена, чтобы не накладываться на выросший список. Проверено через capture-режим: турель выбрана и построена (−2 дерева/−3 камня), боезапас куплен (10), турель 4 раза выстрелила и убила зомби (HP 30→0), боезапас 10→6; на скриншоте турель с подписью «Турель 52/60».
- **2026-06-14** — расширен арсенал до 5 стволов (развитие 4.6.2/4.7.2). К пистолету и дробовику добавлены: **Двойные пистолеты** (урон 9, обойма 16 — вдвое больше, цена 40$), **Автомат** (урон 8, обойма 30, дальность 80, цена 90$) и **Снайперка** (урон 40, обойма 5, дальность 300, цена 120$). Все характеристики и цены — в массиве `weapons` (`player.gd`). Переключение оружия теперь на клавиши **1–5**. Мастерская продаёт оружие прогрессивно по возрастанию цены (клавиша **J** покупает следующий ещё не купленный ствол: двойные пистолеты → дробовик → автомат → снайперка); добавлен `player.next_unowned_weapon_index()`, `workshop.buy_weapon()` берёт следующий по списку. Проверено через capture-режим: до покупки переключение на №2 отклонено, последовательная покупка всех 4 стволов (410→370→320→230→110$, затем «всё оружие уже куплено»), переключение по всем 5 с верными характеристиками (двойные пистолеты 16 патронов, снайперка урон 40/дальность 300, автомат обойма 30, дробовик 5 пуль).
- **2026-06-14** — реализован Этап 4.8.4 (разнообразие турелей: Gatling). Новые `scripts/gatling_turret.gd` (наследует `turret.gd`) + `scenes/gatling_turret.tscn` — тёмно-красная турель с двумя стволами, HP 110 (против 60 у обычной), дальность 16 м (против 12), интервал стрельбы 0.3 с (против 0.8) и урон за выстрел 7 (против 12) — итог: намного выше DPS по одиночной цели, но и расход боезапаса (1 за выстрел) в разы быстрее. Добавлена в систему постройки как «Гатлинг» (12 камня + 6 дерева — дороже мортиры). Дешёвая ранняя «Турель» (4.8.1) осталась как есть — теперь это «Gun Tower», доступная с самого начала, а «Гатлинг» — дорогая основная огневая точка для более позднего этапа игры. Проверено через capture-режим и скриншот: построен за 12 камня/6 дерева; за 1 секунду гатлинг выстрелил 4 раза (HP цели 30→2, почти убил, тогда как обычная турель за то же время успела бы выстрелить 1 раз на 12 урона), боезапас 34→30; на снимке тёмно-красная турель с двумя стволами и подписью «Гатлинг 110/110».
- **2026-06-14** — реализован Этап 4.8.3 (мортирная турель). Новые `scripts/mortar_turret.gd` (наследует `turret.gd`, `extends "res://scripts/turret.gd"`) + `scenes/mortar_turret.tscn` — оливковая турель покрупнее (HP 90 против 60 у обычной), стреляет реже (`fire_interval` 2.5 с против 0.8) и тратит больше боезапаса (`ammo_cost=2`), но при выстреле наносит `turret_damage` (18) по основной цели и `splash_damage` (10) всем врагам в `splash_radius` (3 м) вокруг точки попадания — площадной/навесной огонь, эффективный против скоплений зомби (и, как и обычная турель, без проверки линии видимости — бьёт «из-за стен»). Добавлена в систему постройки (`build_system.gd`) как «Мортира» (8 камня + 4 дерева — дороже обычной турели 3+2), переключается клавишей **V**. Проверено через capture-режим и скриншот: построена за 8 камня/4 дерева; кучка из 3 зомби рядом друг с другом — после двух залпов все три получили урон (HP 30→10→[-1,10,10], один уничтожен), боезапас 20→14; на снимке оливковая мортира с подписью «Мортира 66/90» и HUD-алертом «⚠ Атакована постройка: Мортира!».
- **2026-06-15** — реализован **4.27 (мастерская UI по E + классовые инструменты)** — финал пересмотра базового цикла (4.21–4.27). Мастерская (`workshop.gd`) больше не использует спам-клавиши: внутри зоны клавиша **E** открывает UI-меню (`scenes/workshop_menu.tscn` + `scripts/workshop_menu.gd`, CanvasLayer `process_mode=ALWAYS`) со списком крафта за ресурсы — стена, апгрейд тира и три классовых инструмента; заблокированные по навыку помечены 🔒 с требуемой веткой/уровнем. **Классовые инструменты** (флаги `has_knife`/`has_improved_axe`/`has_hammer` в `InventorySystem`, общий крафт `workshop._craft_tool`/`_tool_spec` с гейтом по `get_skill_level`): Нож (Бой ур.1, +10 урона топора, скорость атаки x0.75), Улучшенный топор (Добыча ур.2, скорость x0.5 — быстрее всех), Молот (Инженер ур.1, ремонт x2 HP + скорость x0.75). У топора добавлен кулдаун удара `axe_swing_interval` (0.6), который инструменты ускоряют (`player._axe_swing_interval`); добыча за удар по-прежнему ТОЛЬКО от навыка «Добыча» (инструменты её не меняют — по решению автора). `craft_hammer` унифицирован через `_craft_tool("hammer")` (теперь гейт Инженер ур.1). Меню добавлено в `main.tscn`. Проверено через capture-режим и скриншот: без навыка «Бой» нож не крафтится (заблокирован=true); с навыками все три инструмента скрафтились (флаги true); интервал удара 0.6→0.3 с улучшенным топором; ремонт молотом HP 10→50 (x2, +40); меню открывается/закрывается; на снимке меню «МАСТЕРСКАЯ» с доступным Ножом и заблокированными 🔒 Улучшенным топором/Молотом.
- **2026-06-15** — реализован **4.26 (UI-меню построек по B)**. По уточнению автора это сделано ПЕРЕД мастерской-меню (4.27). Новые `scenes/build_menu.tscn` + `scripts/build_menu.gd` (CanvasLayer, `process_mode=ALWAYS`): клавиша **B** открывает панель «ПОСТРОЙКИ» со списком из `build_system.get_buildables()` (новый метод) — у каждой постройки имя, цена в ресурсах и гейт по тиру (заблокированные `disabled` + пометка «нужен Тир N»). Клик по постройке зовёт `select_buildable` + входит в режим постройки и закрывает меню (дальше ЛКМ ставит призрак); отдельная кнопка «Выйти из режима постройки». В `player.gd` клавиша **B** теперь открывает меню (в режиме постройки — выходит из него), прежний цикл по **V** (`cycle_buildable`) убран из управления (метод оставлен, но не вызывается). Меню добавлено в `main.tscn` (узел `BuildMenu`). Проверено через capture-режим и скриншот: меню открывается, список из 6 построек, выбор «Турель» включает режим постройки и закрывает меню, выход корректно гасит режим; на снимке Мортира/Гатлинг показаны заблокированными с пометкой «нужен Тир 2/3» (гейтинг тира виден).
- **2026-06-15** — реализован **4.25 (деньги только за ресурсы + удаление боезапаса + переосмысление питания)**. **Боезапас турелей убран целиком**: `turret.gd: _try_fire` больше не тратит/не проверяет `turret_ammo` (стреляет при наличии питания), аналогично `mortar_turret.gd` (убран `ammo_cost`); `gatling_turret.gd` наследует. `turret_ammo` удалён из `InventorySystem.inventory` и `hud.gd` (RESOURCE_NAMES); `Склад` (кормил боезапас) убран из `build_system._buildables`, `storage.gd` лишён подачи боезапаса (оставлен как обычная постройка, будущая роль — хранилище ресурсов). **Деньги — только за оружие** (чёрный рынок, 4.24): из `workshop.gd` удалены `buy_wall`(G)/`buy_heal`(H)/`buy_turret_ammo`(K) и денежные части; апгрейд тира (`upgrade_shelter_tier`) и крафт Молота (`craft_hammer`) теперь стоят только ресурсы. Лечение — только через Лазарет (постройка). **Питание переосмыслено как бюджет мощности** (уточнение автора: электричество — не запас, а «ватты»): `generator.gd` отдаёт фиксированную `power_output` (40; Тир 4 → +20 через `get_power_output`), накопление/`storage_capacity`/таймер производства убраны; `turret.gd: power_cost` (30) — постоянная нагрузка; `_has_power()` суммирует мощность всех генераторов и нарастающую нагрузку турелей по порядку группы — турель запитана, если нагрузка вплоть до неё ≤ мощности, иначе простаивает (остальные работают). `electricity` убран из инвентаря; HUD показывает «Питание: нагрузка / мощность» (`hud.gd: _update_power`, краснеет и шлёт алерт при нехватке). Из `generator.tscn` убран мёртвый экспорт `fuel_consumption_rate`. **Стоимости тиров снижены под кэп ресурсов** (Тир 2: 20д/15с, Тир 3: 30д/25с, Тир 4: 40д/35с) — прежние 50/80 дерева были недостижимы при кэпе 40. Проверено через capture-режим и скриншот: турель/гатлинг убивают цель от питания без боезапаса; без генератора турель простаивает (метка + алерт «НЕТ ПИТАНИЯ»), с генератором снова стреляет; при 1 генераторе (40) из 2 турелей запитана только первая (`[true, false]`); апгрейды тиров 1→2→3→4 проходят за ресурсы, деньги не тронуты; `turret_ammo` отсутствует в инвентаре; на снимке HUD «Питание: 0 / 0», без «Патронов турелей».
- **2026-06-15** — реализован **4.24 (чёрный рынок)**. Перед началом сверено с оригиналом (New Zombie Shelter): чёрный рынок открывается в одной из 3 точек у входа в подвал каждый день, продаёт оружие за деньги (стрельба поднимает «шум» — отложено). У нас: новые `scenes/black_market.tscn` (Area3D-прилавок, тёмно-красный куб + подпись «Чёрный рынок») и `scripts/black_market.gd`. Рынок при старте и при каждом наступлении дня (`day_night_cycle.phase_changed`, подписка отложенная) переезжает в случайную из `spawn_points` (3 точки: (12,0,0)/(-12,0,-8)/(0,0,-14)), стараясь не повторять текущую. Игрок внутри зоны + клавиша **F** — купить следующий ствол за деньги (`buy_weapon` → `player.buy_weapon`, цены/владение в `player.gd`); подсказка показывает следующий ствол и цену. Покупка оружия **из Мастерской убрана** (бывшая клавиша J, функции `buy_weapon`/`_weapon_offer_text` удалены из `workshop.gd`) — деньги тратятся только на рынке. Рынок добавлен в `main.tscn` (узел `BlackMarket`). Проверено через capture-режим и скриншот: весь арсенал куплен на рынке (Двойные пистолеты/Дробовик/Автомат/Снайперка, деньги 400→100$); рынок стартует в точке из списка, за 5 эмулированных «новых дней» точка менялась все 5 раз и всегда оставалась из списка; на снимке прилавок с подписью.
- **2026-06-15** — реализован **4.23 (навыки: очки + меню N)**. Перед началом сверено с оригиналом (New Zombie Shelter): меню навыков по N, на старте 3 очка, +1 за день/ночь, ветки повышают характеристики. У нас (`InventorySystem`): `skill_points` (старт 3), ветки `gather_level` (была с 4.22), `combat_level`, `engineer_level`, константа `SKILL_MAX` (по 3), методы `get_skill_level()`/`upgrade_skill(branch)` (тратит 1 очко, не даёт превысить максимум/уйти в минус) /`add_skill_point()`, сигнал `skills_changed`. Очко **за пережитую ночь** (уточнение автора, а не «за день»): `day_night_cycle._apply_phase` при переходе ночь→день (не стартовая установка) шлёт новый `EventBus.night_survived`; `InventorySystem` подписывается отложенно (он грузится раньше EventBus) и начисляет +1. Эффекты веток: Добыча → +1 ресурс за удар (`resource_pickup.hit`, 4.22); Бой → +10 урона топором (`player.swing_axe`); Инженер → +5 HP к ремонту (`player._repair_building`, плюс открытие крафта инструментов в 4.26). Меню навыков по клавише **N** — новые `scenes/skill_menu.tscn` + `scripts/skill_menu.gd` (CanvasLayer, `process_mode=ALWAYS` чтобы работать на паузе): панель со свободными очками и тремя ветками (уровень `[n/max]` + описание + кнопка `[+]`/`MAX`), кнопки зовут `upgrade_skill`, панель обновляется по `skills_changed`; открытие ставит игру на паузу и освобождает курсор. Проверено через capture-режим и скриншот: старт 3 очка/Добыча 1; вложения дали Добыча 2, Бой 1 (очки 3→1); третье вложение в Инженера потратило последнее очко, четвёртое отклонено (false); `EventBus.night_survived` дал +1 очко; меню открывается/закрывается по `toggle()`; на снимке панель «НАВЫКИ» со списком веток и кнопками.
- **2026-06-15** — реализован **4.22 (добыча топором)**. Узлы дерева/стали у базы (`resource_pickup.gd`, флаг `harvestable=true` на 5 узлах в `main.tscn`) больше не подбираются ходьбой, а **бьются топором** (ЛКМ → `player.swing_axe` → `resource_node.hit()`): за удар выдаётся `InventorySystem.gather_level` ресурса (новое поле, база 1; прокачка 1→2→3 будет в 4.23 «как в оригинале — выше навык, больше за удар»). У узла запас `max_reserve` (10), вычерпался — узел **истощён** (затемняется через `_set_depleted`), восстанавливается до полного **с приходом дня** (подписка на `day_night_cycle.phase_changed`, как реген 3/день в оригинале). Узлы добычи переведены на физический слой 4 (ловятся лучом топора, `collide_with_areas`); дроп ресурсов с зомби (`harvestable=false`) остался подбором ходьбой через `body_entered`; пули проходят сквозь узлы добычи (`player.shoot` исключает слой 4 из маски). Проверено через capture-режим и скриншот: удар при навыке 1 даёт +1 (запас 9/10), при навыке 3 → +3, после вычерпывания `hit()`=0 и `_depleted`=true, после эмуляции дня запас восстановлен до 10; интеграция `swing_axe` лучом по узлу при навыке 2 дала +2; на снимке коричневый узел дерева перед игроком, истощённый узел затемнён, HUD «Дерево: 38».
- **2026-06-15** — стартовал **пересмотр базового цикла по оригиналу** (4.21–4.26, решение автора; нумерация продолжает игровой ЭТАП 4, чтобы не путать с ЭТАПОМ 5 «ассеты»). Перед началом проверена реализация в New Zombie Shelter и по сети (cso.fandom, steamsolo): Shelter Axe — стартовый инструмент для добычи/защиты/ремонта («Hammer больше не нужен»), лучшие инструменты дают больше за удар (нож→2, лом→4); чёрный рынок — 1 из 3 точек ежедневно; навыки по N, очки по дням. Реализован **4.21 (Топор — стартовый инструмент + удар ЛКМ)**: в `player.gd` добавлен `axe_equipped` (на старте топор экипирован), `swing_axe()` пускает луч из камеры (`axe_range` 3 м, `collide_with_areas`) и действует по приоритету — узел ресурса (`resource_node.hit()`, добыча появится в 4.22) → зомби (`take_damage(axe_damage=25)`) → постройка (`repair`); если луч мимо (стены низкие, камера на 1.6 м) — чинит ближайшую постройку в радиусе `repair_range`. **Ремонт стал бесплатным ударом топора** (убраны клавиша **F**, `repair_target`-через-F и `REPAIR_COST` — дерево больше не тратится; Молот по-прежнему даёт x2 HP через `has_hammer`). Топор **без патронов/перезарядки**: `reload()` при топоре игнорируется, HUD показывает «Топор — ближний бой» вместо счётчика (`hud.gd: _on_ammo_changed` при `magazine<0`, `_on_weapon_changed` учитывает `axe_equipped`). Управление: **Q** — взять топор, **1-5** — ствол (снимает топор). Проверено через capture-режим и скриншот: ремонт повреждённой стены +15 HP без траты дерева (26→26), удар по зомби −25 (30→5), R при топоре не восполняет патроны (3→3), HUD показывает «Топор — ближний бой».
- **2026-06-14** — план пересмотрен по итогам детального разбора построек оригинала (новый раздел 17 в `CS_NZ_SHELTER_ANALYSIS.md`: полный каталог построек Shelter и пробелы у нас). В `ROADMAP.md`/`PROGRESS.md` добавлены: **4.8.3** мортирная турель (площадной/непрямой огонь — главный пробел обороны) и **4.8.4** разнообразие турелей (дешёвая ранняя vs дорогая Gatling) — встроены в существующий 4.8 как дешёвые победы; плюс post-MVP-блок **4.14–4.18** (система питания/электричества, тиры убежища 1→4, третий ресурс сталь/металл, тех-постройки Comm Tower/Refinery, Campfire+sanity) — помечены ⏳ как «полная игра», вне MVP. Текущий фокус переставлен: следующим — 4.8.3 (мортира), затем 4.8.4, затем 3.8 (плейтест).
- **2026-06-14** — реализован Этап 4.11 (эвакуация как условие победы, развитие 3.7.1). Победа больше не наступает мгновенно после `victory_waves` (5) волн — `GameStateManager` запускает **финальную фазу эвакуации**: вызывается транспорт, появляется зона эвакуации, идёт обратный отсчёт `evac_time_limit` (45 с). Игрок должен добежать до зоны (победа), иначе «транспорт улетает без вас» (поражение). Новые `scenes/evacuation_zone.tscn`+`scripts/evacuation_zone.gd` — Area3D (детект игрока по слою 2, как мастерская) с ярким эмиссивным бирюзовым маяком-лучом (12 м), зелёной площадкой и подписью «ЗОНА ЭВАКУАЦИИ»; до вызова транспорта скрыта и неактивна (`activate()` показывает и включает `monitoring`). `GameStateManager` получил `_process` (тикает отсчёт, проверяет вход в зону), методы `_start_evacuation()`/`get_evac_time_left()`, флаг `evac_active`, экспорты `evac_time_limit`/`evac_zone_path`. Новый сигнал `EventBus.evacuation_started` → HUD показывает «🚁 ВЫЗВАН ТРАНСПОРТ! Бегите к зоне эвакуации!» и постоянный отсчёт «🚁 Эвакуация: N с — к зелёному лучу!» (`EvacLabel`, краснеет при ≤10 с). Зона размещена за южным проёмом убежища (1.5, 0, 14) — добегать через открытое пространство. Проверено через capture-режим и скриншот: до вызова зона скрыта; после симуляции зачистки 5-й волны зона видима, evac_active=true (45 с), оба HUD-оповещения сработали; игрок добежал → «Эвакуация успешна» → ПОБЕДА (HUD «ПОБЕДА!»); на снимке — яркий бирюзовый маяк ночью с подписью.
- **2026-06-14** — реализован Этап 4.10 (Juggernaut — мини-босс). Новый `scripts/juggernaut.gd` наследует `zombie.gd` (`extends "res://scripts/zombie.gd"`), переиспользуя смерть/дроп/здоровье, но переопределяет ИИ: вместо «иду к игроку, бью постройку лишь упёршись в неё» он **активно ищет ближайшую постройку игрока** (группа `building`) в любой точке карты и крушит её тяжёлыми ударами; если же игрок в радиусе `aggro_radius` (6 м) — переключается на игрока (механика риск/выгода: можно отвести босса от баррикады, встав рядом, но получить урон); без построек просто преследует игрока. Сцена `scenes/juggernaut.tscn` — крупная фиолетовая капсула (радиус 1.0/высота 3.4), HP 300, скорость 1.6, урон по игроку 25, по постройкам 40 (вдвое больше танка), награда 100$, гарантированный дроп. `WaveManager` получил `juggernaut_scene`/`juggernaut_starting_wave` (=3) — с 3-й ночи приходит по одному мини-боссу; он входит в подсчёт волны (волна не зачищена, пока он жив). Новые сигналы `EventBus.juggernaut_spawned`/`juggernaut_defeated` → HUD показывает фиолетовое «⚠ ДЖАГГЕРНАУТ ПРОРЫВАЕТСЯ!» при появлении (эмит в `_ready()`) и зелёное «Джаггернаут повержен!» при гибели. Проверено через capture-режим и скриншот: HP 300; пока игрок далеко — босс разрушил стену (50→0, удары по −40); игрок подошёл вплотную — босс переключился на него (HP игрока 100→75); за убийство +100$; оба HUD-оповещения сработали; на снимке — крупная фиолетовая капсула перед стеной, отличимая от зомби/танка.
- **2026-06-15** — реализован Этап 4.15 (тиры убежища 1→4). В `InventorySystem` добавлен `shelter_tier` (старт 1, максимум `MAX_TIER=4`) и сигнал `tier_changed`. В `Мастерской` новая клавиша **T** — апгрейд тира (`workshop.gd: upgrade_shelter_tier()`), цена растёт с тиром: Тир 2 — 30 дерева/20 камня/80$, Тир 3 — 50/40/150$, Тир 4 — 80/60/250$; при нехватке ресурсов выводится сообщение, на максимуме — «убежище уже максимального тира». `build_system.gd` получил гейтинг по тиру (`min_tier` в каждом buildable): `Мортира` теперь требует Тир 2, `Гатлинг` — Тир 3 (остальные постройки доступны с Тир 1); попытка постройки без нужного тира блокируется с сообщением. Тир 4 даёт бонус генератору — `generator.gd` тратит топливо вдвое медленнее. HUD получил `TierLabel` («Тир убежища: N», под индикатором дня/ночи) и зелёный алерт «🔧 Убежище улучшено до Тир N!» при апгрейде. Проверено через capture-режим и скриншот: на Тир 1 постройка `Мортиры` отклонена («требует Тир 2»), после апгрейда (Тир 1→2, ресурсы и деньги списаны) — построена успешно; `Гатлинг` аналогично заблокирован на Тир 2 и разблокирован на Тир 3; апгрейд до Тир 4 прошёл, повторный апгрейд на максимуме корректно отклонён; на снимке HUD показывает «Тир убежища: 4».
- **2026-06-15** — реализован Этап 4.14 (система питания убежища), плейтест 3.8 пропущен по решению автора. Новые `scripts/generator.gd`+`scenes/generator.tscn` — постройка **Генератор** (HP 70, оранжевый блок с вращающимся вентилятором), группы `building`+`generator`, расходует общий ресурс **топливо** (`fuel`, старт 30, расход 1 за 5 с). В `turret.gd` добавлен метод `_has_power()` (топливо > 0 И есть живой генератор в сцене) и проверка в начале `_physics_process` — при отсутствии питания турель показывает метку «⚡ нет питания» и не наводится/не стреляет; так как `mortar_turret.gd` и `gatling_turret.gd` наследуют `turret.gd` и не переопределяют `_physics_process`, питание требуется для всех типов турелей без дублирования кода. Генератор добавлен в систему постройки (10 камня + 5 дерева, клавиша **V**), предустановлен на арене (`main.tscn`) рядом с точкой `FuelPickup` (+10 топлива). Мастерская продаёт топливо (клавиша **L**, +10 за 15$). Новые сигналы `EventBus.power_lost`/`power_restored` → HUD показывает красное «⚡ НЕТ ПИТАНИЯ — турели простаивают!» и зелёное «⚡ Питание восстановлено — турели снова работают». Проверено через capture-режим и скриншот: с питанием турель убивает цель за 2 выстрела (HP 30→6); при `fuel=0` турель простаивает (HP цели не меняется, видна метка «⚡ нет питания», HUD-алерт красный); после восстановления топлива турель снова стреляет (HP 30→18) и HUD показывает зелёный алерт восстановления.
- **2026-06-15** — реализован Этап 4.17 (часть 1: крафт Молота). Перед началом
  проверено по онлайн-источникам оригинала (cso.fandom/steamsolo, Zombie
  Shelter): Молот крафтится на верстаке (Worktable) и чинит повреждённые
  конструкции быстрее обычного топора. У нас: `InventorySystem.has_hammer`
  (булево, по умолчанию `false`); в Мастерской новая клавиша **M** —
  `workshop.gd: craft_hammer()` тратит 15 дерева + 10 стали + 50$ один раз
  (повторный крафт отклоняется); подсказка верстака показывает цену молота
  или «молот скрафчен (ремонт x2 HP)». В `player.gd: repair_target()` с
  Молотом `repair_amount` (15 HP) умножается на 2 → 30 HP за удар (F).
  Вторую часть 4.17 (Мастерская как ставимая постройка, как турель/генератор
  — `build_system.gd`/`workshop.tscn`) решено вынести в отдельный шаг из-за
  большего объёма переработки сцены. Проверено через capture-режим: молот
  скрафчен (15/10/50 списаны), ремонт повреждённой стены (10/50 HP) дал
  +30 HP вместо обычных +15.
- **2026-06-15** — реализован Этап 4.16 (переработка ресурсов по итогам разд. 18). `InventorySystem`: `stone`→`steel` ("Сталь" в HUD, переименование без новой логики), кэп 40 для дерева/стали (`RESOURCE_CAP`/`CAPPED_RESOURCES`); `fuel`→`electricity` — но, в отличие от первоначального плана разд. 18.2 (собирать электричество как дерево/сталь), по уточнению автора электричество НЕ собирается на карте: его производят генераторы. `generator.gd` переработан с потребления на производство — каждый живой генератор раз в `electricity_production_interval` (4 с, Тир 4 — 2 с) добавляет `electricity_per_tick` (1) в общий запас до своего `storage_capacity` (10); `build_system.gd` ограничивает число генераторов `MAX_GENERATORS=4`. Каждая установленная турель/мортира/гатлинг (`turret.gd`) постоянно тратит электричество просто за то, что стоит — раз в `electricity_drain_interval` (10 с) расходуется 1 ед. из общего запаса, независимо от стрельбы; без запаса или без живого генератора — как и в 4.14, метка «нет питания», `_has_power()` проверяет `electricity > 0`. Все ресурсные пикапы/дроп зомби/стоимости построек/тиров переведены на `steel`; убрана покупка топлива в мастерской (клавиша L). Проверено через capture-режим и скриншот: HUD показывает «Сталь»/«Электричество», турель с питанием стреляет (HP цели 30→6), без электричества простаивает (метка видна, HP не меняется), после восстановления снова стреляет (HP 30→18) — сценарий теста 4.14 воспроизведён на новой модели ресурсов без изменений в ожидаемом поведении.
- **2026-06-14** — реализован Этап 4.9 (HUD: индикаторы угроз). Новая автозагрузка `EventBus` (`scripts/event_bus.gd`) с сигналом `building_damaged(building_name)` — постройки (`wall.gd`, `turret.gd`, `infirmary.gd`, `storage.gd`) шлют его из `take_damage()`, так как создаются динамически и HUD не может подписаться на них напрямую. `WaveManager` получил сигнал `wave_started(wave_number)` (эмитится в начале `start_wave()`, до спавна) в дополнение к существующему `wave_cleared`. HUD (`hud.gd`/`hud.tscn`) получил `AlertLabel` — оповещение по центру сверху экрана, гаснет через 2.5 с: «⚠ Атакована постройка: X!» (красный) при `building_damaged`, «⚠ Волна N: зомби приближаются!» (жёлтый) при старте волны, «Волна N зачищена!» (зелёный) при зачистке. Проверено через capture-режим и скриншот: при повреждении стены поверх HUD появляется красная надпись «⚠ Атакована постройка: Стена!», при старте волны 2 — жёлтое оповещение о волне.
- **2026-06-16** — правки базового цикла по обратной связи автора (этапы 4.28–4.30).
  **4.28 (закрытие меню).** Раньше меню навыков (N), построек (B) и мастерской (E)
  открывались, но не закрывались: при открытии игра ставится на паузу, а
  `player.gd`/`workshop.gd` на паузе ввод не получают (узлы `PAUSABLE`), так что
  повторное нажатие клавиши до них не доходило. Решение: меню и мастерская ловят
  СВОЮ клавишу-закрытие ОПРОСОМ в `_process` (а не через `_unhandled_input`) —
  `process_mode=ALWAYS` работает на паузе. В `skill_menu/build_menu/workshop_menu`
  и `workshop.gd` добавлен фронт нажатия (`_close_key_was_down`/`_e_was_down`):
  повторное нажатие N/B/E или Esc закрывает меню; клавиша, которой меню открыли,
  не считается закрытием (флаг `_close_key_was_down=true` при открытии). Опрос
  вместо виртуала ввода выбран специально — виртуал ввода на `CanvasLayer`/
  рантайм-узле при разрушении дерева цепляет `get_viewport()==null`.
  **4.29 (добыча — физические удары + случайный спавн).** Узел добычи
  (`resource_pickup.gd`, `harvestable=true`) теперь имеет случайный запас УДАРОВ
  (HP) из диапазона `min_hits..max_hits` (5–7); за КАЖДЫЙ удар топором выдаётся
  `gather_level` ресурса (навык «Добыча», 1/2/3): дерево с 6 ударами при добыче 1
  даст 6 ресурса, при добыче 2 — 12. Дневной реген «на том же месте» УБРАН: узел
  исчерпался → шлёт сигнал `depleted` и исчезает. Новый `scripts/resource_spawner.gd`
  + `scenes/resource_node.tscn` (узел-«дерево» 0.8×1.8×0.8, слой 4 — ловится лучом
  топора): спавнер держит на карте заданное число узлов дерева/стали, ставит их в
  СЛУЧАЙНЫХ точках в кольце вокруг базы (`min_radius..max_radius`, не ближе
  `min_separation` друг к другу) и по сигналу `depleted` заменяет исчезнувший узел
  новым в другой случайной точке. Из `main.tscn` убраны 5 фиксированных узлов
  ресурсов, добавлен `ResourceSpawner` (3 дерева + 3 стали). Узел сжимается по
  высоте по мере выработки (`_update_visual`).
  **4.30 (мастерскую и генератор строит игрок).** На старте их больше НЕТ — оба
  стали ставимыми постройками (`build_system._buildables`: «Мастерская» 10д/5с,
  «Генератор» уже был), `workshop_scene` добавлен в `BuildSystem` (`player.tscn`).
  Вторую мастерскую ставить нельзя (проверка в `try_place`). Из `main.tscn` убраны
  предустановленные `Workshop` и `Generator`. `workshop.gd` переведён с
  `_unhandled_input` на опрос E в `_process` (см. 4.28): теперь это рантайм-узел.
  **Исправления крашей прогона (только тестовый харнесс, на игру не влияют):**
  (1) Рантайм-`Label3D` (подсказки/HP над мастерской/постройками) при резком
  `get_tree().quit()` разрушались уже после окна и роняли процесс
  (`label_3d.cpp: window==null`, сигнал 11) — в `debug_capture.gd` прогон теперь
  завершается через `OS.kill(OS.get_process_id())` сразу после скриншота, минуя
  разбор дерева (в самой игре выход штатный: рестарт — `reload_current_scene`,
  окно при этом живо). (2) В `build_system._make_ghost` `Label3D` призрака теперь
  ПРЯЧЕТСЯ (`visible=false`), а не `queue_free` — освобождение узла до входа в
  дерево (`add_child` отложен) давало гонку/use-after-free и недетерминированный
  краш по ходу прогона. (3) В дроп-тесте добавлена проверка `is_instance_valid`
  перед `in` (узлы освобождались по ходу итерации). Проверено через capture-режим
  (8/8 прогонов EXIT=0, без segfault) и скриншот: на старте мастерской/генератора
  нет (0/0); игрок строит мастерскую (вторая отклонена) и генератор; добыча — узел
  с запасом 6 ударов, +1 при навыке 1 и +3 при навыке 3, исчерпался и заменён
  спавнером (узлов на карте 6→6), `swing_axe` по узлу при навыке 2 дал +2; экран
  ПОБЕДА, HUD «Топор — ближний бой», «Питание 0/0».
- **2026-06-16** — правки по обратной связи автора (этап 4.31, 7 пунктов).
  **(1) Паузы в игре нет.** Меню навыков/построек/мастерской больше НЕ ставят
  `get_tree().paused` — только освобождают курсор для кликов; игра идёт, зомби
  не замирают. Раз паузы нет, player/workshop всегда получают ввод, поэтому
  `_process`-опрос закрытия (костыль под паузу) убран — открытие/закрытие меню
  снова простым `toggle()` по клавише в `player.gd`/`workshop.gd`. Esc закрывает
  любое открытое меню (player `_close_all_menus`); пока меню открыто, ЛКМ не
  стреляет и не перехватывает курсор (`_any_menu_open`); клавиши открытия — с
  `not event.echo`. **(2) Баг урона по зомби (регресс 4.22).** Зомби в
  `zombie.tscn` на `collision_layer=4` — на ТОМ ЖЕ слое, что узлы добычи
  (`HARVEST_LAYER` был 4), а `player.shoot` исключал слой 4 из маски, чтобы пули
  проходили сквозь дерево/сталь → пули проходили и сквозь зомби («Попадание»
  печаталось, зомби невредим; вдобавок `hits += 1` стоял ВНЕ проверки
  `take_damage` — ложное «Попадание» при выстреле в землю/стену). Узлы добычи
  переведены на слой 16 (`HARVEST_LAYER=16`, `resource_node.tscn`), `shoot`
  исключает слой 16 (`0xFFFFFFEF`) → пуля попадает по зомби (слой 4) и проходит
  сквозь узлы; `hits++` теперь только при реальном `take_damage`. **(3) Спавн
  ресурсов вне базы и не на стенах.** `resource_spawner.gd`: кольцо вынесено ЗА
  периметр убежища (`min_radius=11`, периметр на ±8), каждая точка проходит физ.
  оверлап-чек сферой по слою 1 (стены/периметр/постройки) — занятые точки
  отвергаются; спавн отложен до `physics_frame` (иначе space state не готов).
  **(4) Случайное число спотов.** Было фиксировано 6 — теперь дерево
  `randi_range(3,5)`, сталь `randi_range(2,4)`; места и так случайны (угол+радиус
  в кольце). **(5) Тексты не сквозь стены и не издалека.** У всех мировых Label3D
  (HP построек, подсказки мастерской/рынка) убран `no_depth_test=true` (теперь
  скрыты геометрией) и задан `visibility_range_end=12` (не видно дальше 12 м от
  камеры — видно при подходе); метка эвакуации не тронута (туда бежать — видна
  издалека). **(6) Нельзя строить на постройку.** `build_system.try_place`:
  физ. оверлап-чек сферой (r=0.45) в точке призрака по слою 1 — если занято
  (постройка/стена/периметр), ставить нельзя. **(7) Гейт ресурсов в меню.**
  `build_menu`: кнопка постройки гаснет (`disabled`) с пометкой «не хватает
  ресурсов» прямо в меню (`_can_afford`), а не при попытке поставить. Проверено
  через capture-режим: выстрел из пистолета по зомби сквозь узел добычи —
  HP 30→20 (урон прошёл); узлы добычи на старте — случайное число, мин. радиус
  от центра 11–12 (вне базы), позиции случайные; вторая стена на ту же клетку
  отклонена; питание `[true,false]`; меню навыков открывается/закрывается; на
  снимке HUD «Пистолет — Патроны 7/8», «Питание 0/0», экран ПОБЕДА. Замечание:
  в прогоне сохраняется пред-существующая редкая (~10%) флакость движка в
  baseline-секции дропа (массовый спавн/смерть танков) — не связана с правками,
  на игру не влияет; при падении прогон перезапускается.
